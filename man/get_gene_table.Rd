% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{get_gene_table}
\alias{get_gene_table}
\title{Function to map gene IDs to gene symbols, Entrez IDs, and Ensembl IDs}
\usage{
get_gene_table(gene_ids, orgdb = NULL, keytype = NULL)
}
\arguments{
\item{gene_ids}{A character vector of gene IDs.}

\item{orgdb}{An \code{OrgDb} object to map the IDs with, or \code{NULL} to only use the IDs themselves (per \code{keytype}).}

\item{keytype}{A string giving the type of \code{gene_ids}, as a \code{keytype} of \code{orgdb}, e.g. \code{'ENTREZID'}, \code{'ENSEMBL'}, or \code{'FLYBASE'}. Versions of Ensembl IDs are ignored. If \code{NULL}, the type is unknown.}
}
\value{
A \code{data.frame} with one row per unique gene ID, and columns \code{gene_id}, \code{symbol}, \code{entrez_id}, and \code{ensembl_id}.
}
\description{
When an ID maps to more than one symbol, Entrez ID, or Ensembl ID, the first is used.
}
