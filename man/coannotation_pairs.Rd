% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualize.R
\name{coannotation_pairs}
\alias{coannotation_pairs}
\title{Function to find the pairs of annotation types on each region}
\usage{
coannotation_pairs(tbl)
}
\arguments{
\item{tbl}{A \code{data.frame} of annotated regions, from \code{as.data.frame()} on the result of \code{annotate_regions()}.}
}
\value{
A \code{data.frame} with columns \code{seqnames}, \code{start}, \code{end}, \code{V1}, and \code{V2}, with one row per distinct pair of annotation types on each region.
}
\description{
A region with one annotation gives the pair of that type with itself. A region with more than one annotation gives each pair of its annotations, with the types of each pair in sorted order, so a region annotated to a CpG island and a promoter counts only toward the island / promoter pair, and not toward island / island or promoter / promoter.
}
