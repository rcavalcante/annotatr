% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{list_cached_annotations}
\alias{list_cached_annotations}
\title{List cached annotations and downloads}
\usage{
list_cached_annotations()
}
\value{
A \code{data.frame} with one row per cached item, and columns \code{type} (\code{'annotation'} or \code{'download'}), \code{genome}, \code{name} (the annotation code or URL), \code{sources} (the versions an annotation was built with), \code{size_mb}, \code{added}, and \code{path}.
}
\description{
List the annotations and downloaded files in the cache used by \code{build_annotations()}. See \code{\link{cached-annotations}} for more about the cache.
}
\examples{
# Use a temporary cache so the example doesn't change your cache
old_options = options(annotatr.cache = tempfile())

list_cached_annotations()

options(old_options)

}
