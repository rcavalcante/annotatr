% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache.R
\name{clear_cached_annotations}
\alias{clear_cached_annotations}
\title{Clear cached annotations and downloads}
\usage{
clear_cached_annotations(genome = NULL)
}
\arguments{
\item{genome}{A character vector of genome assemblies to remove, e.g. \code{'hg19'}. If \code{NULL} (the default), everything is removed.}
}
\value{
A \code{data.frame} of the removed items, as from \code{list_cached_annotations()}, invisibly.
}
\description{
Remove annotations and downloaded files from the cache used by \code{build_annotations()}, so they will be built or downloaded again. See \code{\link{cached-annotations}} for more about the cache.
}
\examples{
# Use a temporary cache so the example doesn't change your cache
old_options = options(annotatr.cache = tempfile())

# Remove the hg19 annotations and downloads
clear_cached_annotations(genome = 'hg19')

# Remove everything
clear_cached_annotations()

options(old_options)

}
